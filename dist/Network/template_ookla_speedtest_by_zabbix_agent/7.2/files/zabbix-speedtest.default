# Installed to /etc/default/zabbix-speedtest
# Optional: pin to a specific Ookla server ID. Leave unset for auto-select.
# Replace the example ID below with one from `speedtest -L` on this host.
# SPEEDTEST_SERVER_ID=23969
